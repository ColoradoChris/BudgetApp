<% include ../partials/header %>
<% var options = ["Gas", "Groceries", "Rent/Mortgage", "Restaurants", "Haircut/Grooming", "Entertainment", "Clothing", "Gifts", "Learning/Development", "Music", "Car Payments", "Cell Phone", "Toiletries", "Travel", "Health/Doctor"]; %>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Edit an Expense</h1>
            <form action="/expense/<%= expense._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="type">Type:</label>
                    <select class="form-control" name="type">
                        <% options.forEach(function(option){ %>
                            <% if(expense.type === option){ %>
                                <option value="<%= option %>" selected="selected"><%= option %></option>
                            <% } else { %>
                                <option value="<%= option %>"><%= option %></option>
                                <% } %>
                        <% }); %>
                    </select>
                </div>
                <div class="form-group">
                    <label for="amount">Amount:</label>
                    <input class="form-control" type="number" name="amount" min="0.01" step="0.01" value="<%= expense.amount %>">
                </div>
                <div class="form-group">
                    <label for="date">Date:</label>
                    <input class="form-control" type="date" name="date" value="<%= date.format(expense.date, "YYYY-MM-DD") %>">
                </div>
                <div class="form-group">
                    <button class="form-control btn btn-success">Submit!</button>
                </div>
            </form>
            <a href="/dashboard" class="btn btn-primary">Back to Dashboard</a>
        </div>
    </div>
</div>

<% include ../partials/footer %>